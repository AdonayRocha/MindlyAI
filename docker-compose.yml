# docker-compose.yml (versionless) — running mindly only and using host-installed Ollama

services:
  mindly:
    build: .
    container_name: mindly
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
